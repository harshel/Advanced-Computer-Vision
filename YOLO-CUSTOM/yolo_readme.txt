The zip file contains following files

1. A folder named 'img' which consists of all the images and the corresponding annotations

2. 'obj.dat' - which contains classes= 2
				train = data/train.txt
				valid = data/train.txt	
				names = data/obj.names
				backup = backup/

3. 'obj.names' - Which contains the classes names

4. 'train.txt' - Which contains the path to the images in the format
		data/img/image_name.jpg